{
  "name": "aws-lambda-nodejs",
  "version": "1.0.0",
  "description": "",
  "author": "Nguyen Van Long",
  "license": "MIT",
  "dependencies": {
    "uuid": "^2.0.3"
  },
  "scripts": {
    "invoke-populate": "serverless invoke local -f --stage dev --region us-east-1 populate",
    "invoke-list": "serverless invoke local -f --stage dev --region us-east-1 list",
    "invoke-query": "serverless invoke --f query --stage dev --region us-east-1 --data '{"name": "mac","email": "mac@example"}"
  }
}
